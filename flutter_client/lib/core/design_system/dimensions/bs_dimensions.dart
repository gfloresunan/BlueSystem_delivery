/// BLUE SYSTEM DELIVERY ENTERPRISE — DESIGN SYSTEM DIMENSIONS
/// Canonical touch targets, icon sizes, and component dimensions with iOS Safe ergonomics.

class BSDimensions {
  BSDimensions._();

  // ─── Touch Targets (Apple HIG & Material 3 minimum) ─────────────────────
  static const double minTouchTarget = 44.0;
  static const double standardTouchTarget = 48.0;

  // ─── Avatar Sizes ────────────────────────────────────────────────────────
  static const double avatarXs = 28.0;
  static const double avatarSm = 36.0;
  static const double avatarMd = 46.0;
  static const double avatarLg = 60.0;
  static const double avatarXl = 80.0;

  // ─── Icon Sizes ──────────────────────────────────────────────────────────
  static const double iconXs = 14.0;
  static const double iconSm = 18.0;
  static const double iconMd = 24.0;
  static const double iconLg = 28.0;
  static const double iconXl = 36.0;
  static const double iconGiant = 64.0;

  // ─── Button Heights ──────────────────────────────────────────────────────
  static const double buttonHeightSm = 36.0;
  static const double buttonHeightMd = 44.0;
  static const double buttonHeightLg = 52.0;

  // ─── Input Heights ───────────────────────────────────────────────────────
  static const double inputHeight = 52.0;
  static const double searchBarHeight = 46.0;

  // ─── Navigation & App Shell ──────────────────────────────────────────────
  static const double bottomNavHeight = 64.0;
  static const double centralCartFabSize = 64.0;
  static const double centralCartIconSize = 28.0;

  // ─── Cards & Banners ─────────────────────────────────────────────────────
  static const double bannerHeight = 160.0;
  static const double merchantCardImageHeight = 135.0;
  static const double merchantCardWidth = 260.0;
  static const double productPromoCardWidth = 160.0;
  static const double productPromoImageHeight = 100.0;
  static const double categoryPillHeight = 40.0;
}
